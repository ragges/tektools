; Indentation settings for C in Emacs
; tektool.c is written in several different indentation styles, but the
; style mostly used seems quite close to Emacs "linux" style.
;((c-mode . ((c-file-style . "linux")
;	    (subdirs . nil)))
; )
(("tektool.c" . ((c-mode . ((c-file-style . "linux"))))))
